/*
 * support for dealing with ADIF logs
 *
 * ADIF is an ugly, ugly format..
 *
 * Here we support appending an entry to the ADIF log and exporting the logbook.
 *
 * Some day we'll support importing ADIF into the logbook database too
 */
#include <libied/cfg.h>
#include <libied/debuglog.h>
